# DELETE /api/1/invite/{invite_id}

Cancel invitation

Returns a `200` if the invitation was successfully canceled.

## Parameters

### Path Parameters

| Name        | Type      | Required | Description |
| ----------- | --------- | -------- | ----------- |
| `invite_id` | `integer` | Yes      | -           |

## Responses

### 200 — Invitation cancelled
