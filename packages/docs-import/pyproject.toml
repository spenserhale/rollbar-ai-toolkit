[project]
name = "rollbar-docs-import"
version = "0.1.0"
description = "Import Rollbar's AI-ready documentation (llms.txt + .md) into docs/api/."
requires-python = ">=3.11"
dependencies = ["httpx>=0.27"]
